[package]
name = "glimpse_core"
description = "Core types and traits for the glimpse capture library."
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
thiserror.workspace = true
